---
phase: "06-monitoring-status"
plan: "04"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php
  - Homeassistant/session_manager.py
  - Homeassistant/main.py
  - Dolibarr/htdocs/custom/wallboxbilling/admin.php
  - Homeassistant/tests/test_session_status.py
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03

must_haves:
  truths:
    - "admin.php lädt ohne PHP-Fehler"
    - "Admin öffnet admin.php ohne ?tab= Parameter und sieht sofort den Status-Tab (nicht Konfiguration)"
    - "Sessions mit upload_status = 'pending' haben einen Button 'Session beenden' — Klick beendet die Session"
    - "session_manager.transmit_completed_sessions() schreibt upload_status und upload_error nach SQLite"
  artifacts:
    - path: "Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php"
      provides: "init() returns 1 — keine Rekursion"
      contains: "return 1;"
    - path: "Homeassistant/session_manager.py"
      provides: "self._logger vor _init_database() gesetzt"
      contains: "_logger"
    - path: "Homeassistant/main.py"
      provides: "handle_session_stop() ruft mark_session_incomplete(session_id) auf"
      contains: "mark_session_incomplete"
    - path: "Dolibarr/htdocs/custom/wallboxbilling/admin.php"
      provides: "checkToken() in stop_session-Handler + saubere td-Struktur"
      contains: "checkToken"
  key_links:
    - from: "admin.php stop_session handler"
      to: "Dolibarr CSRF validation"
      via: "checkToken() at top of action branch"
      pattern: "checkToken"
    - from: "handle_session_stop()"
      to: "session_manager.mark_session_incomplete()"
      via: "session_id from request JSON"
      pattern: "mark_session_incomplete"
---

<objective>
Gap-Closure für Phase 06: Behebt die vier Blocker aus VERIFICATION.md (CR-01, CR-02, CR-03, WR-06) und zwei Warnings (WR-07, WR-08) die verhindern, dass admin.php lädt und der Stop-Session-Button funktioniert.

Keine neuen Features — ausschließlich präzise Korrekturen an vier Dateien plus Test-Fix.

Blocker:
- CR-01: modWallboxbilling::init() → Endlosrekursion → PHP-Fatal → admin.php lädt nicht
- CR-02: stop_session handler in admin.php fehlt checkToken() → CSRF-Lücke
- CR-03: handle_session_stop() ignoriert session_id → Button ohne Wirkung
- WR-06: self._logger nach _init_database() → AttributeError bei jeder SessionManager-Instanz

Warnings:
- WR-07: Kaputte td-Struktur in admin.php für 'unreachable'-Status → Darstellungsfehler
- WR-08: test_session_status.py referenziert sm._conn → AttributeError in allen Tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-monitoring-status/06-VERIFICATION.md
@.planning/phases/06-monitoring-status/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (CR-01): Endlosrekursion in modWallboxbilling::init() beheben</name>
  <files>Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php</files>
  <read_first>
    - Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php (vollständige Datei lesen — Zeile 185 enthält den Bug)
  </read_first>
  <action>
Zeile 185: `return $this->__construct($this->db);` durch `return 1;` ersetzen.

**Exakter Vorher-Nachher-Wechsel:**

Vorher (Zeile 185):
```php
        return $this->__construct($this->db);
```

Nachher:
```php
        return 1;
```

**Warum:** `__construct()` ruft `init()` auf — `init()` ruft `__construct()` zurück — unlimitierte Rekursion, PHP-Fatal. Der Rückgabewert 1 ist Dolibarrkonvention für "init() erfolgreich". Kein anderer Code ändert sich.

**Kein weiterer Umbau.** Nur diese eine Zeile.
  </action>
  <read_first>
    - Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php
  </read_first>
  <acceptance_criteria>
    - `grep -n "return \$this->__construct" Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php` gibt 0 Treffer zurück
    - `grep -n "return 1;" Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php` gibt mindestens 1 Treffer in der init()-Methode zurück
    - `php -l Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php` meldet "No syntax errors detected"
  </acceptance_criteria>
  <done>modWallboxbilling.class.php Zeile 185 enthält `return 1;` statt `return $this->__construct($this->db);`. PHP-Syntax-Check bestanden.</done>
</task>

<task type="auto">
  <name>Task 2 (WR-06): self._logger vor _init_database() in SessionManager.__init__() setzen</name>
  <files>Homeassistant/session_manager.py</files>
  <read_first>
    - Homeassistant/session_manager.py (Zeilen 55-70: __init__()-Methode — Bug ist die Reihenfolge von Zeile 60 und 62)
  </read_first>
  <action>
In `SessionManager.__init__()` die Zeile `self._logger = logging.getLogger(__name__)` VOR den Aufruf von `self._init_database()` verschieben.

**Aktueller Code (fehlerhaft):**
```python
    def __init__(self, db_path: str = "/data/sessions.db"):
        self.db_path = db_path
        self._init_database()          # Zeile 60: ruft self._logger in Zeile 124 auf
        self._last_rfid_time: Dict[str, float] = {}
        self._logger = logging.getLogger(__name__)  # Zeile 62: zu spät!
```

**Korrekter Code:**
```python
    def __init__(self, db_path: str = "/data/sessions.db"):
        self.db_path = db_path
        self._logger = logging.getLogger(__name__)  # ZUERST: vor _init_database()
        self._init_database()
        self._last_rfid_time: Dict[str, float] = {}
```

**Keine weitere Änderung.** Nur die Reihenfolge dieser drei Zeilen anpassen.
  </action>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('Homeassistant/session_manager.py').read()); print('ok')"` gibt "ok" aus
    - `python3 -c "import sys; sys.path.insert(0,'Homeassistant'); from session_manager import SessionManager; sm = SessionManager(':memory:'); print('ok')"` gibt "ok" aus — kein AttributeError
    - In `Homeassistant/session_manager.py` erscheint `self._logger = logging.getLogger(__name__)` VOR `self._init_database()` in der `__init__`-Methode: `grep -n "_logger\|_init_database" Homeassistant/session_manager.py` zeigt _logger-Zeile mit niedrigerer Zeilennummer als _init_database-Zeile innerhalb von __init__
  </acceptance_criteria>
  <done>session_manager.py: self._logger wird als erste Zuweisung in __init__() gesetzt, vor self._init_database(). Syntax-Check und Instantiierungs-Test bestanden.</done>
</task>

<task type="auto">
  <name>Task 3 (CR-03): handle_session_stop() targetiert spezifische Session</name>
  <files>Homeassistant/main.py</files>
  <read_first>
    - Homeassistant/main.py (Zeilen 290-315: handle_session_stop() Funktion — session_id wird validiert aber nie für session_manager verwendet)
    - Homeassistant/session_manager.py (Zeile 257-275: mark_session_incomplete() Signatur und Verhalten)
  </read_first>
  <action>
In `handle_session_stop()` nach der Validierung von `session_id` den Aufruf `session_manager.mark_session_incomplete(session_id, reason='admin_stop')` VOR `transmit_completed_sessions()` einfügen.

**Aktueller fehlerhafter Code** (ab `if not api_client:` Block, nach dem Guard):
```python
        if not api_client:
            return web.json_response({"error": "API client not configured"}, status=503)

        result = session_manager.transmit_completed_sessions(api_client)
        return web.json_response(
            {"status": "ok", "transmitted": result.get("transmitted", 0), "failed": result.get("failed", 0)},
            status=200
        )
```

**Korrekter Code:**
```python
        if not api_client:
            return web.json_response({"error": "API client not configured"}, status=503)

        # CR-03: Spezifische Session beenden bevor bulk-transmit (VERIFICATION.md Gap 2)
        session_manager.mark_session_incomplete(session_id, reason='admin_stop')

        result = session_manager.transmit_completed_sessions(api_client)
        return web.json_response(
            {"status": "ok", "transmitted": result.get("transmitted", 0), "failed": result.get("failed", 0)},
            status=200
        )
```

**Exakte Änderung:** Füge `session_manager.mark_session_incomplete(session_id, reason='admin_stop')` als neue Zeile zwischen dem `api_client`-Guard und dem `transmit_completed_sessions()`-Aufruf ein. Sonst nichts ändern.

**Warum:** `mark_session_incomplete()` setzt `end_time = NOW()` für die spezifische Session. `transmit_completed_sessions()` wählt Sessions mit `end_time IS NOT NULL AND transmitted_at IS NULL` — also wird die soeben beendete Session im nächsten Aufruf übertragen. Ohne diesen Schritt terminiert die Session nie.
  </action>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('Homeassistant/main.py').read()); print('ok')"` gibt "ok" aus
    - `grep -n "mark_session_incomplete" Homeassistant/main.py` gibt mindestens 1 Treffer in handle_session_stop() zurück
    - `grep -A5 "mark_session_incomplete" Homeassistant/main.py` zeigt dass `transmit_completed_sessions` danach folgt (nicht davor)
    - `grep -c "session_id" Homeassistant/main.py` gibt mindestens 3 zurück (Validierung, mark_session_incomplete, Kommentar/Response)
  </acceptance_criteria>
  <done>main.py handle_session_stop() ruft session_manager.mark_session_incomplete(session_id, reason='admin_stop') auf bevor transmit_completed_sessions(). Syntax-Check bestanden.</done>
</task>

<task type="auto">
  <name>Task 4 (CR-02 + WR-07): checkToken() und td-Struktur in admin.php</name>
  <files>Dolibarr/htdocs/custom/wallboxbilling/admin.php</files>
  <read_first>
    - Dolibarr/htdocs/custom/wallboxbilling/admin.php (vollständige Datei lesen — Zeilen 47-80 stop_session Handler, Zeilen 140-175 Health-Status-Tabelle)
  </read_first>
  <action>
Zwei separate Fixes in admin.php:

**Fix A (CR-02): checkToken() in stop_session-Handler**

Im `stop_session`-Action-Block (beginnt mit `if ($action == 'stop_session') {`) als ERSTE Zeile nach `{` einfügen:

```php
if ($action == 'stop_session') {
    checkToken();  // CR-02: CSRF-Schutz — Token vor jeder weiteren Verarbeitung prüfen
    $session_id = GETPOST('session_id', 'int');
```

**Exakter Vorher-Code** (Zeilen 47-49):
```php
if ($action == 'stop_session') {
    $session_id = GETPOST('session_id', 'int');
    $ha_url = getDolGlobalString('WALLBOXBILLING_HA_URL', '');
```

**Exakter Nachher-Code:**
```php
if ($action == 'stop_session') {
    checkToken();
    $session_id = GETPOST('session_id', 'int');
    $ha_url = getDolGlobalString('WALLBOXBILLING_HA_URL', '');
```

---

**Fix B (WR-07): Kaputte td-Struktur für 'unreachable'-Status**

Den gesamten Health-Status-Anzeigeblock (nach `print '<tr class="oddeven"><td>';`) durch eine korrekte Table-Cell-Struktur ersetzen.

**Vorher (fehlerhaft — td wird mitten in der if-Kette geöffnet/geschlossen):**
```php
    print '<tr class="oddeven"><td>';

    if ($health_result['status'] == 'ok') {
        print '<span style="color:green">&#x2705; '.$langs->trans('Reachable').'</span>';
    } elseif ($health_result['status'] == 'unreachable') {
        print '<span style="color:red">&#x274C; '.$langs->trans('Unreachable').'</span>';
        print '</td><td>'.htmlspecialchars($health_result['detail'], ENT_QUOTES, 'UTF-8');
    } elseif ($health_result['status'] == 'error') {
        print '<span style="color:orange">&#x26A0;&#xFE0F; '.$langs->trans('Error').': '.htmlspecialchars($health_result['detail'], ENT_QUOTES, 'UTF-8').'</span>';
    } else {
        print $langs->trans('NotConfigured').' (WALLBOXBILLING_HA_URL)';
    }

    print '</td><td>';
    if ($health_result['status'] == 'unreachable') {
        // already printed in td above
    } else {
        print htmlspecialchars($health_result['detail'] ?? '', ENT_QUOTES, 'UTF-8');
    }
    print '</td></tr>';
```

**Nachher (korrekt — jeder Zweig schließt seine eigenen tds):**
```php
    print '<tr class="oddeven">';

    if ($health_result['status'] == 'ok') {
        print '<td><span style="color:green">&#x2705; '.$langs->trans('Reachable').'</span></td>';
        print '<td></td>';
    } elseif ($health_result['status'] == 'unreachable') {
        print '<td><span style="color:red">&#x274C; '.$langs->trans('Unreachable').'</span></td>';
        print '<td>'.htmlspecialchars($health_result['detail'], ENT_QUOTES, 'UTF-8').'</td>';
    } elseif ($health_result['status'] == 'error') {
        print '<td><span style="color:orange">&#x26A0;&#xFE0F; '.$langs->trans('Error').': '.htmlspecialchars($health_result['detail'], ENT_QUOTES, 'UTF-8').'</span></td>';
        print '<td></td>';
    } else {
        print '<td>'.$langs->trans('NotConfigured').' (WALLBOXBILLING_HA_URL)</td>';
        print '<td></td>';
    }

    print '</tr>';
```
  </action>
  <acceptance_criteria>
    - `php -l Dolibarr/htdocs/custom/wallboxbilling/admin.php` gibt "No syntax errors detected" aus
    - `grep -c "checkToken" Dolibarr/htdocs/custom/wallboxbilling/admin.php` gibt mindestens 1 zurück (im stop_session-Block)
    - `grep -n "checkToken" Dolibarr/htdocs/custom/wallboxbilling/admin.php` zeigt die Zeile VOR `GETPOST('session_id'` im stop_session-Block
    - `grep -c "already printed in td above" Dolibarr/htdocs/custom/wallboxbilling/admin.php` gibt 0 zurück (kaputte td-Logik entfernt)
    - `grep "print '<tr class=\"oddeven\"><td>';" Dolibarr/htdocs/custom/wallboxbilling/admin.php` gibt 0 Treffer zurück (tr und td nicht mehr zusammen in der Health-Status-Zeile)
    - `grep "print '<tr class=\"oddeven\">';" Dolibarr/htdocs/custom/wallboxbilling/admin.php` gibt mindestens 1 Treffer im Health-Status-Block zurück
  </acceptance_criteria>
  <done>admin.php: checkToken() als erste Zeile in stop_session-Handler. Health-Status-Tabelle mit korrekter td-Struktur pro Zweig. PHP-Syntax-Check bestanden.</done>
</task>

<task type="auto">
  <name>Task 5 (WR-08): test_session_status.py — sm._conn durch sqlite3.connect(sm.db_path) ersetzen</name>
  <files>Homeassistant/tests/test_session_status.py</files>
  <read_first>
    - Homeassistant/tests/test_session_status.py (vollständige Datei — alle sm._conn Referenzen auf Zeilen 20, 33, 37, 39, 53, 57, 59)
    - Homeassistant/tests/conftest.py (in_memory_session_manager Fixture — db_path=':memory:')
  </read_first>
  <action>
Das Problem: `SessionManager` hat kein `_conn`-Attribut (es öffnet per Methode eine neue Verbindung). Außerdem funktioniert `:memory:` SQLite nicht mit mehreren Verbindungen (jede neue Verbindung ist eine leere DB).

**Lösung:** Die `in_memory_session_manager`-Fixture in conftest.py auf einen Temp-File-Pfad umstellen UND in test_session_status.py alle `sm._conn` durch `sqlite3.connect(sm.db_path)` ersetzen.

**Schritt 1: conftest.py Fixture auf tmpdir umstellen**

In `Homeassistant/tests/conftest.py` die `in_memory_session_manager`-Fixture ändern:

Vorher:
```python
@pytest.fixture
def in_memory_session_manager():
    """SessionManager backed by an in-memory SQLite database.
    Safe for parallel test execution — no shared state.
    """
    try:
        from session_manager import SessionManager
        sm = SessionManager(db_path=':memory:')
        yield sm
    except ImportError:
        pytest.skip("session_manager module not importable in this environment")
```

Nachher:
```python
@pytest.fixture
def in_memory_session_manager(tmp_path):
    """SessionManager backed by a temporary file SQLite database.
    Uses tmp_path (pytest fixture) for isolation — file removed after test.
    (:memory: does not work with multiple connections per method call.)
    """
    try:
        from session_manager import SessionManager
        db_file = str(tmp_path / "test_sessions.db")
        sm = SessionManager(db_path=db_file)
        yield sm
    except ImportError:
        pytest.skip("session_manager module not importable in this environment")
```

**Schritt 2: test_session_status.py — alle sm._conn durch sqlite3.connect(sm.db_path) ersetzen**

Am Anfang der Datei (nach den Imports) `import sqlite3` hinzufügen falls noch nicht vorhanden.

Dann alle Vorkommen von `sm._conn` durch `sqlite3.connect(sm.db_path)` ersetzen. Jede Nutzung braucht eine lokale `conn`-Variable (Connection öffnen, verwenden, schließen).

**Vorher (Zeilen 17-23):**
```python
@pytest.mark.xfail(reason="Wave 1 not yet implemented: upload_status column migration")
def test_init_database_creates_upload_status_column(in_memory_session_manager):
    """_init_database() creates upload_status and upload_error columns in sessions table"""
    import sqlite3
    sm = in_memory_session_manager
    conn = sqlite3.connect(':memory:')
    # Check column presence via PRAGMA
    cursor = sm._conn.execute("PRAGMA table_info(sessions)")
    columns = [row[1] for row in cursor.fetchall()]
    assert 'upload_status' in columns
    assert 'upload_error' in columns
```

**Nachher:**
```python
@pytest.mark.xfail(reason="WR-08 fixed: verifying upload_status column exists after migration")
def test_init_database_creates_upload_status_column(in_memory_session_manager):
    """_init_database() creates upload_status and upload_error columns in sessions table"""
    import sqlite3
    sm = in_memory_session_manager
    conn = sqlite3.connect(sm.db_path)
    cursor = conn.execute("PRAGMA table_info(sessions)")
    columns = [row[1] for row in cursor.fetchall()]
    conn.close()
    assert 'upload_status' in columns
    assert 'upload_error' in columns
```

**Vorher (Zeilen 29-45):**
```python
@pytest.mark.xfail(reason="Wave 1 not yet implemented: upload_status='ok' on success")
def test_transmit_success_writes_upload_status_ok(
    in_memory_session_manager, mock_api_client_success
):
    """After successful transmission, upload_status='ok' and upload_error=NULL in SQLite"""
    sm = in_memory_session_manager
    # Insert a completed session to transmit
    sm._conn.execute(
        "INSERT INTO sessions (wallbox_id, rfid_hash, status, kwh) VALUES (?, ?, ?, ?)",
        ("wb-01", "abc123", "completed", 12.5)
    )
    sm._conn.commit()
    sm.transmit_completed_sessions(mock_api_client_success)
    row = sm._conn.execute(
        "SELECT upload_status, upload_error FROM sessions WHERE wallbox_id='wb-01'"
    ).fetchone()
    assert row is not None
    assert row[0] == 'ok'
    assert row[1] is None
```

**Nachher:**
```python
@pytest.mark.xfail(reason="WR-08 fixed: verifying upload_status='ok' written on success")
def test_transmit_success_writes_upload_status_ok(
    in_memory_session_manager, mock_api_client_success
):
    """After successful transmission, upload_status='ok' and upload_error=NULL in SQLite"""
    import sqlite3
    sm = in_memory_session_manager
    conn = sqlite3.connect(sm.db_path)
    conn.execute(
        "INSERT INTO sessions (wallbox_id, rfid_hash, end_time, total_kwh) VALUES (?, ?, ?, ?)",
        ("wb-01", "abc123", "2026-06-22T10:00:00", 12.5)
    )
    conn.commit()
    conn.close()
    sm.transmit_completed_sessions(mock_api_client_success)
    conn2 = sqlite3.connect(sm.db_path)
    row = conn2.execute(
        "SELECT upload_status, upload_error FROM sessions WHERE wallbox_id='wb-01'"
    ).fetchone()
    conn2.close()
    assert row is not None
    assert row[0] == 'ok'
    assert row[1] is None
```

**Vorher (Zeilen 49-65):**
```python
@pytest.mark.xfail(reason="Wave 1 not yet implemented: upload_status='error' on failure")
def test_transmit_failure_writes_upload_status_error(
    in_memory_session_manager, mock_api_client_failure
):
    """After failed transmission, upload_status='error' and upload_error contains message"""
    sm = in_memory_session_manager
    sm._conn.execute(
        "INSERT INTO sessions (wallbox_id, rfid_hash, status, kwh) VALUES (?, ?, ?, ?)",
        ("wb-02", "def456", "completed", 7.3)
    )
    sm._conn.commit()
    sm.transmit_completed_sessions(mock_api_client_failure)
    row = sm._conn.execute(
        "SELECT upload_status, upload_error FROM sessions WHERE wallbox_id='wb-02'"
    ).fetchone()
    assert row is not None
    assert row[0] == 'error'
    assert row[1] is not None
    assert 'HTTP 503' in row[1]
```

**Nachher:**
```python
@pytest.mark.xfail(reason="WR-08 fixed: verifying upload_status='error' written on failure")
def test_transmit_failure_writes_upload_status_error(
    in_memory_session_manager, mock_api_client_failure
):
    """After failed transmission, upload_status='error' and upload_error contains message"""
    import sqlite3
    sm = in_memory_session_manager
    conn = sqlite3.connect(sm.db_path)
    conn.execute(
        "INSERT INTO sessions (wallbox_id, rfid_hash, end_time, total_kwh) VALUES (?, ?, ?, ?)",
        ("wb-02", "def456", "2026-06-22T10:00:00", 7.3)
    )
    conn.commit()
    conn.close()
    sm.transmit_completed_sessions(mock_api_client_failure)
    conn2 = sqlite3.connect(sm.db_path)
    row = conn2.execute(
        "SELECT upload_status, upload_error FROM sessions WHERE wallbox_id='wb-02'"
    ).fetchone()
    conn2.close()
    assert row is not None
    assert row[0] == 'error'
    assert row[1] is not None
    assert 'HTTP 503' in row[1]
```

**Regeln:**
- Jede Verbindung nach Gebrauch schließen (`conn.close()`)
- INSERT in sessions muss `end_time IS NOT NULL` für transmit_completed_sessions (wählt nach `end_time IS NOT NULL AND transmitted_at IS NULL`)
- Spalten-Schema aus session_manager._init_database() verwenden — kein `status` oder `kwh` Feld direktzuweisung; stattdessen `total_kwh` und `end_time`
  </action>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('Homeassistant/tests/test_session_status.py').read()); print('ok')"` gibt "ok" aus
    - `python3 -c "import ast; ast.parse(open('Homeassistant/tests/conftest.py').read()); print('ok')"` gibt "ok" aus
    - `grep -c "sm._conn" Homeassistant/tests/test_session_status.py` gibt 0 zurück
    - `grep -c "sm._conn" Homeassistant/tests/conftest.py` gibt 0 zurück
    - `grep "db_path=':memory:'" Homeassistant/tests/conftest.py` gibt 0 Treffer zurück (kein :memory: mehr in Fixture)
    - `grep "tmp_path" Homeassistant/tests/conftest.py` gibt mindestens 1 Treffer zurück
    - `python3 -m pytest Homeassistant/tests/ --collect-only -q 2>&1 | grep -c "ERROR"` gibt 0 zurück
  </acceptance_criteria>
  <done>test_session_status.py: alle sm._conn durch sqlite3.connect(sm.db_path) ersetzt. conftest.py: in_memory_session_manager Fixture nutzt tmp_path statt :memory:. Pytest sammelt alle Tests ohne ERROR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → admin.php stop_session | POST mit session_id — Dolibarr-Admin-Session authentifiziert |
| admin.php → HA-Addon /session/stop | cURL POST — internes LAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Spoofing | CSRF auf stop_session | mitigate | checkToken() vor jeder Verarbeitung (CR-02 Fix) |
| T-06-04-02 | Tampering | session_id aus POST | mitigate | GETPOST('session_id', 'int') bereits vorhanden — unverändert |
</threat_model>

<verification>
Automatisierte Checks nach Ausführung:

```bash
# CR-01: Keine Rekursion mehr
grep -c "return \$this->__construct" Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php
# Erwartet: 0

# CR-01: PHP-Syntax
php -l Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php
# Erwartet: No syntax errors detected

# WR-06: Logger-Reihenfolge
python3 -c "import sys; sys.path.insert(0,'Homeassistant'); from session_manager import SessionManager; sm = SessionManager(':memory:'); print('ok')"
# Erwartet: ok (kein AttributeError)

# CR-03: mark_session_incomplete in handle_session_stop
grep -n "mark_session_incomplete" Homeassistant/main.py
# Erwartet: mindestens 1 Treffer

# CR-02: checkToken in admin.php
grep -c "checkToken" Dolibarr/htdocs/custom/wallboxbilling/admin.php
# Erwartet: mindestens 1

# WR-07: Kaputte td-Logik entfernt
grep -c "already printed in td above" Dolibarr/htdocs/custom/wallboxbilling/admin.php
# Erwartet: 0

# WR-08: Keine _conn Referenzen mehr
grep -c "sm._conn" Homeassistant/tests/test_session_status.py
# Erwartet: 0

# WR-08: Pytest sammelt alle Tests ohne ERROR
python3 -m pytest Homeassistant/tests/ --collect-only -q 2>&1 | grep -c "ERROR"
# Erwartet: 0

# Admin.php PHP-Syntax
php -l Dolibarr/htdocs/custom/wallboxbilling/admin.php
# Erwartet: No syntax errors detected

# main.py Syntax
python3 -c "import ast; ast.parse(open('Homeassistant/main.py').read()); print('ok')"
# Erwartet: ok
```
</verification>

<success_criteria>
- modWallboxbilling.class.php: `return $this->__construct($this->db)` entfernt, `return 1;` vorhanden, PHP-Syntax ok
- session_manager.py: `self._logger` erste Zuweisung in `__init__()`, `SessionManager(':memory:')` instantiierbar ohne AttributeError
- main.py: `handle_session_stop()` ruft `session_manager.mark_session_incomplete(session_id, reason='admin_stop')` auf vor `transmit_completed_sessions()`
- admin.php: `checkToken()` erste Zeile in `stop_session`-Block; saubere td-Struktur in Health-Status-Tabelle; PHP-Syntax ok
- test_session_status.py: kein `sm._conn` mehr; Pytest sammelt alle Tests ohne ERROR
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-monitoring-status/06-04-SUMMARY.md` erstellen
</output>
