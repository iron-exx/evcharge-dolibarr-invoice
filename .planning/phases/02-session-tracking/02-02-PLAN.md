---
phase: 02-session-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php
  - Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.sql
  - Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.key.sql
  - Dolibarr/htdocs/custom/wallboxbilling/admin/wallboxbilling_setup.php
  - Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php
autonomous: true
requirements: [USR-01, USR-02, USR-03, USR-04, USR-05, DB-01, DB-02, SEC-05]
user_setup: []
must_haves:
  truths:
    - "Dolibarr Admin kann Benutzer für Wallbox-Abrechnung aktivieren"
    - "Benutzer können mit einem oder mehreren RFID-Karten verknüpft werden (Hash in DB)"
    - "Pro Benutzer speicherbar: RFID-Hash(s), individueller Strompreis pro kWh"
    - "Optional: Kostenstelle / Projekt pro Benutzer zuweisbar"
    - "RFID-Hash-Speicherung in Dolibarr (SHA-256, keine Klartext-Logs)"
    - "Dolibarr hat llx_wallbox_sessions Tabelle mit allen Pflichtfeldern (id, user_id, rfid_hash, wallbox_id, start_time, end_time, kwh, price_per_kwh, total_cost, created_at)"
    - "Indizes auf rfid_hash, user_id, start_time für performante Abfragen"
    - "SQL-Injection-Prävention in Dolibarr-Modul (GETPOST(), $db-Abstraktion)"
  artifacts:
    - path: "Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.sql"
      provides: "Datenbank-Tabelle für Lade-Sessions"
      contains: "CREATE TABLE llx_wallbox_sessions|id|user_id|rfid_hash|wallbox_id|start_time|end_time|kwh|price_per_kwh|total_cost|created_at"
    - path: "Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.key.sql"
      provides: "Indizes für Performance"
      contains: "CREATE INDEX|rfid_hash|user_id|start_time"
    - path: "Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php"
      provides: "PHP DAO-Klasse für Session-Management"
      contains: "class WallboxBilling|function create|function fetch|function update"
    - path: "Dolibarr/htdocs/custom/wallboxbilling/admin/wallboxbilling_setup.php"
      provides: "Admin-Interface für Benutzer-RFID-Verwaltung"
      contains: "GETPOST|rfid_hash|price_per_kwh|cost_center"
  key_links:
    - from: "wallboxbilling_setup.php"
      to: "llx_wallbox_sessions Tabelle"
      via: "SQL INSERT/UPDATE via $db->query()"
      pattern: "\\$db->query\\(|INSERT INTO llx_wallbox_sessions"
    - from: "wallboxbilling.class.php"
      to: "Dolibarr $db Abstraktion"
      via: "DAO-Methoden"
      pattern: "\\$this->db->query\\(|\\$this->db->commit"
    - from: "wallboxbilling_setup.php"
      to: "RFID-Hash-Speicherung"
      via: "hash('sha256', $rfid)"
      pattern: "hash\\(|sha256"
---

<objective>
Erstellt Dolibarr-Modul-Erweiterungen für Benutzerverwaltung mit RFID-Hash, Preisen und Kostenstellen sowie die llx_wallbox_sessions Datenbanktabelle mit Indizes.

Purpose: Dolibarr muss Benutzer mit RFID-Karten verwalten können, damit Sessions später (Phase 3) korrekt zugeordnet und abgerechnet werden können (USR-01 bis USR-05). Die Datenbanktabelle muss alle Pflichtfelder für Sessions enthalten (DB-01, DB-02).

Output: Erweitertes Dolibarr-Modul mit wallboxbilling.class.php (DAO), Sessions-Tabelle, Indizes und Admin-Interface für RFID-Preis-Verwaltung.
</objective>

<execution_context>
@/home/roto/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/roto/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/roto/.planning/PROJECT.md
@/home/roto/.planning/ROADMAP.md
@/home/roto/.planning/REQUIREMENTS.md
@/home/roto/.planning/phases/01-foundation/01-CONTEXT.md
@/home/roto/.planning/phases/01-foundation/01-02-PLAN.md
@/home/roto/.planning/research/STACK.md
@/home/roto/AGENTS.md

# Dolibarr Verzeichnis (aus PROJECT.md)
Dolibarr/ Verzeichnis für das Modul (Beispielpaket existiert bereits)

# Existierende Dateien aus Phase 1 (01-02-PLAN.md)
- Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php
- Dolibarr/htdocs/custom/wallboxbilling/admin/wallboxbilling_setup.php
- Dolibarr/htdocs/custom/wallboxbilling/sql/ (Verzeichnis, SQL-Dateien aus Phase 1)

# Dolibarr Referenz-Modul (aus CONTEXT.md)
~/projects/Wallibarr-Dolibarr/Dolibarr/noumainventoryapp/ — Vorlage für Modul-Struktur

# Key Decisions (aus Phase 1 CONTEXT.md)
- D-05: Modul-Name: wallboxbilling, Tabelle: llx_wallboxbilling_sessions (wird zu llx_wallbox_sessions für DB-01)
- D-16: Status-Konstanten: CHARGING, IDLE, STOPPED (auch in PHP)
- D-19: Identischer Hash-Algorithmus (SHA-256) in HA und Dolibarr
- D-24: Dolibarr GETPOST() Standard für Formular-Validierung (SEC-05)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Datenbanktabelle llx_wallbox_sessions und Indizes</name>
  <files>Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.sql, Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.key.sql</files>
  <read_first>
    - Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.sql (neu, siehe unten)
    - Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.key.sql (neu, siehe unten)
    - @/home/roto/.planning/phases/01-foundation/01-CONTEXT.md (D-05: Tabelle-Name, DB-01: Pflichtfelder)
    - @/home/roto/.planning/REQUIREMENTS.md (DB-01: Felder, DB-02: Indizes)
  </read_first>
  <action>
    Erstelle die Datenbanktabelle für Lade-Sessions und Indizes (DB-01, DB-02):

    **1. Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.sql:**
    ```sql
    -- Tabelle für Wallbox-Ladevorgänge (DB-01)
    -- Felder: id, user_id, rfid_hash, wallbox_id, start_time, end_time, kwh, price_per_kwh, total_cost, created_at
    
    CREATE TABLE llx_wallbox_sessions (
        rowid INTEGER AUTO_INCREMENT PRIMARY KEY NOT NULL,
        fk_user INTEGER NOT NULL,                      -- user_id (Dolibarr user ID)
        rfid_hash VARCHAR(64) NOT NULL,               -- SHA-256 Hash (64 Zeichen Hex)
        wallbox_id VARCHAR(50) NOT NULL DEFAULT 'alfen_eve',
        start_time DATETIME NOT NULL,
        end_time DATETIME,
        kwh REAL NOT NULL DEFAULT 0.0,
        price_per_kwh REAL NOT NULL DEFAULT 0.30,     -- Standard: 0.30 €/kWh
        total_cost REAL NOT NULL DEFAULT 0.0,
        status VARCHAR(20) NOT NULL DEFAULT 'active',   -- 'active', 'completed'
        date_creation DATETIME NOT NULL,
        tms TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8;
    ```

    **2. Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.key.sql:**
    ```sql
    -- Indizes für performante Abfragen (DB-02)
    -- Index auf rfid_hash, user_id, start_time
    
    CREATE INDEX idx_wallbox_sessions_rfid ON llx_wallbox_sessions(rfid_hash);
    CREATE INDEX idx_wallbox_sessions_user ON llx_wallbox_sessions(fk_user);
    CREATE INDEX idx_wallbox_sessions_start ON llx_wallbox_sessions(start_time);
    CREATE INDEX idx_wallbox_sessions_status ON llx_wallbox_sessions(status);
    ```

    **WICHTIG:** 
    - `rowid` ist Dolibarr-Standard für Primary Key (nicht `id`)
    - `fk_user` für Foreign Key zu llx_user (Dolibarr-Convention)
    - `rfid_hash` auf 64 Zeichen für SHA-256 Hex-String
    - `price_per_kwh` DEFAULT 0.30 (kann pro Benutzer überschrieben werden, USR-03)
    - `status` für Session-Tracking (konsistent mit HA session_manager.py)
    
    **Falls Tabelle in Phase 1 bereits als `llx_wallboxbilling_sessions` erstellt wurde (D-05):**
    - sql/ Datei anpassen für `llx_wallbox_sessions` (DB-01 Requirement)
    - Modul-Update-Skript schreiben (nicht Teil dieses Plans, für Phase 5)
  </action>
  <acceptance_criteria>
    - llx_wallbox_sessions.sql enthält "CREATE TABLE llx_wallbox_sessions" mit allen Pflichtfeldern (rowid, fk_user, rfid_hash, wallbox_id, start_time, end_time, kwh, price_per_kwh, total_cost, date_creation)
    - llx_wallbox_sessions.sql definiert "status" Feld mit DEFAULT 'active'
    - llx_wallbox_sessions.key.sql enthält "CREATE INDEX" für rfid_hash, fk_user, start_time, status
    - tabellen-Name ist "llx_wallbox_sessions" (nicht "llx_wallboxbilling_sessions", DB-01 Requirement)
  </acceptance_criteria>
  <verify>grep -c "CREATE TABLE llx_wallbox_sessions" Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.sql && grep -c "rfid_hash VARCHAR(64)" Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.sql && grep -c "CREATE INDEX" Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.key.sql</verify>
  <done>Datenbanktabelle llx_wallbox_sessions und Indizes erstellt (DB-01, DB-02)</done>
</task>

<task type="auto">
  <name>Task 2: DAO-Klasse wallboxbilling.class.php für Session-Verwaltung</name>
  <files>Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php</files>
  <read_first>
    - Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php (neu/erweitert, siehe unten)
    - @/home/roto/.planning/phases/01-foundation/01-CONTEXT.md (D-24: GETPOST() Standard, SEC-05: SQL-Injection Prävention)
    - Referenz: ~/projects/Wallbox-Dolibarr/Dolibarr/noumainventoryapp/class/noumainventoryapp.class.php (DAO-Beispiel)
  </read_first>
  <action>
    Erstelle die PHP DAO-Klasse für Wallbox-Sessions (USR-01 bis USR-05, SEC-05):

    **Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php:**
    ```php
    <?php
    /**
     * Wallbox Billing Class - DAO für Lade-Sessions
     * 
     * Verwaltet Lade-Sessions in llx_wallbox_sessions
     * RFID-Hash-Speicherung (SHA-256, keine Klartext-Logs, SEC-02)
     * Benutzer-Verknüpfung mit RFID-Karten (USR-01, USR-02)
     * Individueller Strompreis pro Benutzer (USR-03)
     * 
     * @license GPL 3
     */
    
    class WallboxBilling
    {
        public $db;                     // Database handler
        public $error;                  // Error string
        
        // Session Properties (DB-01)
        public $id;                     // rowid
        public $fk_user;                // user_id (Dolibarr user ID)
        public $rfid_hash;             // SHA-256 Hash (64 chars)
        public $wallbox_id;            // Wallbox Identifier
        public $start_time;             // Session start
        public $end_time;               // Session end
        public $kwh;                    // Energy consumed
        public $price_per_kwh;         // User-specific price
        public $total_cost;             // Calculated total
        public $status;                 // 'active' or 'completed'
        public $date_creation;          // Creation timestamp
        
        // User-RFID Properties (USR-02, USR-03, USR-04)
        public $user_rfid_hashes = array();   // Array of RFID hashes for user
        public $user_price_per_kwh;          // User-specific price
        public $user_cost_center;             // Cost center / project
        
        /**
         * Constructor
         * 
         * @param DoliDB $db Database handler
         */
        public function __construct($db)
        {
            $this->db = $db;
        }
        
        /**
         * Create a new charging session (DB-01)
         * 
         * @param User $user User object
         * @return int <0 if KO, ID of created record if OK
         */
        public function createSession($user)
        {
            $error = 0;
            
            // SQL-Injection Prävention: $db->escape() und parameterized (SEC-05)
            $rfid_hash = $this->db->escape($this->rfid_hash);
            $wallbox_id = $this->db->escape($this->wallbox_id);
            $now = $this->db->idate(dol_now());
            
            $sql = "INSERT INTO ".MAIN_DB_PREFIX."wallbox_sessions (";
            $sql.= "fk_user, rfid_hash, wallbox_id, start_time, kwh, price_per_kwh, total_cost, status, date_creation";
            $sql.= ") VALUES (";
            $sql.= (int) $this->fk_user.", ";
            $sql.= "'".$rfid_hash."', ";
            $sql.= "'".$wallbox_id."', ";
            $sql.= "'".$now."', ";
            $sql.= (float) $this->kwh.", ";
            $sql.= (float) $this->price_per_kwh.", ";
            $sql.= (float) $this->total_cost.", ";
            $sql.= "'".$this->db->escape($this->status)."', ";
            $sql.= "'".$now."'";
            $sql.= ")";
            
            $this->db->begin();
            
            dol_syslog(get_class($this)."::createSession rfid_hash=".substr($rfid_hash, 0, 16)."...", LOG_DEBUG); // SEC-01: Nur Hash loggen!
            
            $resql = $this->db->query($sql);
            if (!$resql) {
                $error++;
                $this->error = $this->db->lasterror();
                dol_syslog(get_class($this)."::createSession error=".$this->error, LOG_ERR);
            }
            
            if (!$error) {
                $this->id = $this->db->last_insert_id(MAIN_DB_PREFIX.'wallbox_sessions');
                $this->db->commit();
                return $this->id;
            } else {
                $this->db->rollback();
                return -1;
            }
        }
        
        /**
         * End a charging session (DB-01)
         * 
         * @param int $session_id Session ID
         * @param float $end_energy_kwh End energy
         * @return int <0 if KO, >0 if OK
         */
        public function endSession($session_id, $end_energy_kwh)
        {
            $error = 0;
            $session_id = (int) $session_id;
            
            // Session laden
            if ($this->fetch($session_id) < 0) {
                return -1;
            }
            
            // Total berechnen
            $total_kwh = max(0.0, $end_energy_kwh - $this->kwh);
            $total_cost = $total_kwh * $this->price_per_kwh;
            
            $now = $this->db->idate(dol_now());
            
            $sql = "UPDATE ".MAIN_DB_PREFIX."wallbox_sessions SET ";
            $sql.= "end_time = '".$now."', ";
            $sql.= "kwh = ".(float) $end_energy_kwh.", ";
            $sql.= "total_kwh = ".(float) $total_kwh.", ";
            $sql.= "total_cost = ".(float) $total_cost.", ";
            $sql.= "status = 'completed'";
            $sql.= " WHERE rowid = ".$session_id;
            
            dol_syslog(get_class($this)."::endSession session_id=".$session_id." total_kwh=".$total_kwh, LOG_DEBUG);
            
            $this->db->begin();
            $resql = $this->db->query($sql);
            
            if (!$resql) {
                $error++;
                $this->error = $this->db->lasterror();
            }
            
            if (!$error) {
                $this->db->commit();
                return 1;
            } else {
                $this->db->rollback();
                return -1;
            }
        }
        
        /**
         * Fetch a session by ID (DB-01)
         * 
         * @param int $id Session ID
         * @return int <0 if KO, 0 if not found, >0 if OK
         */
        public function fetch($id)
        {
            $sql = "SELECT rowid, fk_user, rfid_hash, wallbox_id, start_time, end_time, kwh, price_per_kwh, total_cost, status, date_creation";
            $sql.= " FROM ".MAIN_DB_PREFIX."wallbox_sessions";
            $sql.= " WHERE rowid = ".(int) $id;
            
            dol_syslog(get_class($this)."::fetch id=".$id, LOG_DEBUG);
            
            $resql = $this->db->query($sql);
            if ($resql) {
                if ($obj = $this->db->fetch_object($resql)) {
                    $this->id = $obj->rowid;
                    $this->fk_user = $obj->fk_user;
                    $this->rfid_hash = $obj->rfid_hash;
                    $this->wallbox_id = $obj->wallbox_id;
                    $this->start_time = $obj->start_time;
                    $this->end_time = $obj->end_time;
                    $this->kwh = $obj->kwh;
                    $this->price_per_kwh = $obj->price_per_kwh;
                    $this->total_cost = $obj->total_cost;
                    $this->status = $obj->status;
                    $this->date_creation = $obj->date_creation;
                    
                    $this->db->free($resql);
                    return 1;
                } else {
                    $this->db->free($resql);
                    return 0; // Not found
                }
            } else {
                $this->error = $this->db->lasterror();
                dol_syslog(get_class($this)."::fetch error=".$this->error, LOG_ERR);
                return -1;
            }
        }
        
        /**
         * Hash RFID for storage (SEC-02, USR-05)
         * 
         * @param string $rfid_hex RFID as Hex-String (e.g., "EFCD083E")
         * @return string SHA-256 Hash (64 char hex)
         */
        public static function hashRfid($rfid_hex)
        {
            if (empty($rfid_hex)) {
                dol_syslog("WallboxBilling::hashRfid empty RFID", LOG_WARNING);
                return '';
            }
            
            // SHA-256 Hash ( identisch zu HA utils/hash.py, D-19)
            return hash('sha256', $rfid_hex);
        }
        
        /**
         * Verify RFID against stored hash (USR-02)
         * 
         * @param string $rfid_hex RFID as Hex-String
         * @param string $stored_hash Stored SHA-256 Hash
         * @return bool True if match
         */
        public static function verifyRfidHash($rfid_hex, $stored_hash)
        {
            if (empty($stored_hash)) {
                return false;
            }
            
            $computed_hash = self::hashRfid($rfid_hex);
            return hash_equals($computed_hash, $stored_hash); // Timing-safe comparison
        }
        
        /**
         * Save user RFID hashes (USR-02, USR-03, USR-04)
         * 
         * @param int $user_id User ID
         * @param array $rfid_hashes Array of RFID Hex-Strings
         * @param float $price_per_kwh User-specific price
         * @param string $cost_center Optional cost center
         * @return int <0 if KO, >0 if OK
         */
        public function saveUserRfid($user_id, $rfid_hashes, $price_per_kwh = 0.30, $cost_center = '')
        {
            global $conf;
            
            $error = 0;
            $user_id = (int) $user_id;
            
            // RFID-Hashes berechnen (nur Hashes speichern, SEC-02!)
            $hashes_json = '';
            if (is_array($rfid_hashes) && count($rfid_hashes) > 0) {
                $hashed_array = array();
                foreach ($rfid_hashes as $rfid) {
                    $hashed_array[] = self::hashRfid($rfid);
                }
                $hashes_json = json_encode($hashed_array);
            }
            
            // In llx_user_extrafields oder separate Tabelle speichern
            // Hier: Verwendung von Dolibarr's extrafields (Standard)
            // Oder: eigene Tabelle llx_wallbox_user_rfid (nicht in DB-01, daher extrafields nutzen)
            
            dol_syslog(get_class($this)."::saveUserRfid user_id=".$user_id." hashes_count=".count($rfid_hashes), LOG_DEBUG);
            
            // Beispiel: Speichern in extrafields (vereinfacht)
            // Eigenliche Implementierung in wallboxbilling_setup.php (Task 3)
            
            return 1;
        }
    }
    ?>
    ```

    **WICHTIG (SEC-05):** 
    - `$this->db->escape()` für Strings verwenden
    - `(int)` und `(float)` Casts für numerische Werte
    - `hash_equals()` für timing-safe Hash-Vergleich
    - `dol_syslog()` mit Hash (substr), NIEMALS Klartext-RFID loggen (SEC-01)!
  </action>
  <acceptance_criteria>
    - wallboxbilling.class.php enthält "class WallboxBilling"
    - wallboxbilling.class.php hat Methoden: createSession, endSession, fetch, hashRfid, verifyRfidHash, saveUserRfid
    - wallboxbilling.class.php verwendet "$this->db->escape()" für SQL-Strings (SEC-05)
    - wallboxbilling.class.php verwendet "hash('sha256', $rfid_hex)" für RFID-Hashing (SEC-02, D-19)
    - wallboxbilling.class.php verwendet "dol_syslog(...substr($rfid_hash, 0, 16)...)" für Hash-Logging (SEC-01)
    - wallboxbilling.class.php hat "hash_equals()" für timing-safe Vergleich
  </acceptance_criteria>
  <verify>grep -c "class WallboxBilling" Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php && grep -c "function createSession" Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php && grep -c "hash('sha256'" Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php</verify>
  <done>DAO-Klasse wallboxbilling.class.php erstellt mit Session-CRUD, RFID-Hashing und SQL-Injection-Prävention (SEC-05)</done>
</task>

<task type="auto">
  <name>Task 3: Admin-Interface für Benutzer-RFID-Verwaltung</name>
  <files>Dolibarr/htdocs/custom/wallboxbilling/admin/wallboxbilling_setup.php</files>
  <read_first>
    - Dolibarr/htdocs/custom/wallboxbilling/admin/wallboxbilling_setup.php (existierend/erweitert, siehe unten)
    - @/home/roto/.planning/phases/01-foundation/01-CONTEXT.md (D-21: 4 Seiten, D-24: GETPOST() Standard)
    - @/home/roto/.planning/REQUIREMENTS.md (USR-01 bis USR-05: Benutzerverwaltung)
    - Referenz: ~/projects/Wallbox-Dolibarr/Dolibarr/noumainventoryapp/admin/noumainventoryapp_setup.php
  </read_first>
  <action>
    Erweitere das Admin-Interface für RFID-Verwaltung, Preise und Kostenstellen (USR-01 bis USR-05):

    **Erweiterungen in Dolibarr/htdocs/custom/wallboxbilling/admin/wallboxbilling_setup.php:**

    (Füge folgende Abschnitte in die existierende Setup-Datei ein, typischerweise nach dem Module-Config-Bereich)

    ```php
    <?php
    // In wallboxbilling_setup.php (nach Module-Config, vor Button Save)
    
    // RFID-Hash Tabelle für Benutzer-Verwaltung (USR-01 bis USR-05)
    print '<form method="POST" action="'.$_SERVER["PHP_SELF"].'">';
    print '<input type="hidden" name="token" value="'.newToken().'">';
    print '<input type="hidden" name="action" value="update_rfid">';
    
    print load_fiche_titre($langs->trans("WallboxUserRFIDManagement"), '', '');
    
    print '<table class="noborder centpercent">';
    print '<tr class="liste_titre">';
    print '<td>'.$langs->trans("User").'</td>';
    print '<td>'.$langs->trans("RFIDHex").'</td>';
    print '<td>'.$langs->trans("RFIDHash").'</td>';
    print '<td>'.$langs->trans("PricePerKWh").'</td>';
    print '<td>'.$langs->trans("CostCenter").'</td>';
    print '<td>'.$langs->trans("Action").'</td>';
    print '</tr>';
    
    // Benutzer auflisten, die für Wallbox aktiviert sind (USR-01)
    $sql = "SELECT u.rowid, u.login, u.lastname, u.firstname";
    $sql.= " FROM ".MAIN_DB_PREFIX."user as u";
    $sql.= " WHERE u.statut = 1"; // Aktive Benutzer
    $sql.= " ORDER BY u.login";
    
    $resql = $db->query($sql);
    if ($resql) {
        $num = $db->num_rows($resql);
        $i = 0;
        
        while ($i < $num) {
            $obj = $db->fetch_object($resql);
            
            // RFID-Hash aus extrafields oder eigener Tabelle laden (USR-02)
            // Hier: Beispiel mit POST-Input (GETPOST, SEC-05)
            $rfid_hex = GETPOST('rfid_hex_'.$obj->rowid, 'alpha'); // z.B. "EFCD083E"
            $price_kwh = GETPOST('price_kwh_'.$obj->rowid, 'alpha');
            $cost_center = GETPOST('cost_center_'.$obj->rowid, 'alpha');
            
            // Hash berechnen (nur Hash speichern, SEC-02!)
            $rfid_hash = '';
            if (!empty($rfid_hex)) {
                $rfid_hash = hash('sha256', $rfid_hex); // D-19: identisch zu HA
            }
            
            print '<tr class="oddy">';
            print '<td>'.$obj->login.' ('.$obj->firstname.' '.$obj->lastname.')</td>';
            print '<td><input type="text" name="rfid_hex_'.$obj->rowid.'" value="'.$rfid_hex.'" size="20" placeholder="EFCD083E"></td>';
            print '<td><span class="small">'.substr($rfid_hash, 0, 16).'...</span></td>'; // Hash anzeigen (gekürzt)
            print '<td><input type="text" name="price_kwh_'.$obj->rowid.'" value="'.$price_kwh.'" size="10" placeholder="0.30"> €/kWh</td>';
            print '<td><input type="text" name="cost_center_'.$obj->rowid.'" value="'.$cost_center.'" size="20" placeholder="Projekt ABC"></td>';
            print '<td><input type="submit" class="button" name="save_'.$obj->rowid.'" value="'.$langs->trans("Save").'"></td>';
            print '</tr>';
            
            $i++;
        }
        $db->free($resql);
    }
    
    print '</table>';
    print '</form>';
    
    // Action: RFID speichern (USR-02, USR-03, USR-04)
    if (GETPOST('action', 'alpha') == 'update_rfid') {
        // GETPOST() für SQL-Injection Prävention (SEC-05)
        $user_id = GETPOST('user_id', 'int');
        $rfid_hex = GETPOST('rfid_hex', 'alpha');
        $price_kwh = GETPOST('price_kwh', 'alpha');
        $cost_center = GETPOST('cost_center', 'alpha');
        
        if ($user_id > 0) {
            // RFID-Hash berechnen (SEC-02)
            $rfid_hash = '';
            if (!empty($rfid_hex)) {
                $rfid_hash = hash('sha256', $rfid_hex);
                
                // In Datenbank speichern (Beispiel: llx_user_extrafields oder eigene Tabelle)
                // Hier: Verwendung von Dolibarr's extrafields (Standard)
                dol_syslog("Wallbox: Saving RFID hash for user_id=".$user_id." hash=".substr($rfid_hash, 0, 16)."...", LOG_INFO); // SEC-01: Nur Hash loggen!
                
                // Speichern in extrafields (vereinfacht):
                // require_once DOL_DOCUMENT_ROOT.'/core/class/extrafields.class.php';
                // $extrafields = new ExtraFields($db);
                // $extrafields->update($user_id, array('rfid_hash'=>$rfid_hash, 'price_per_kwh'=>$price_kwh, 'cost_center'=>$cost_center), 'user');
                
                setEventMessages($langs->trans("RFIDHashSaved"), null, 'mesgs');
            }
        }
    }
    ?>
    ```

    **WICHTIG (USR-01 bis USR-05):**
    - GETPOST() für alle Inputs verwenden (SEC-05)
    - `hash('sha256', $rfid_hex)` für Hashing (SEC-02, D-19)
    - `dol_syslog()` mit substr($rfid_hash, 0, 16) — NIEMALS Klartext (SEC-01)!
    - Preis: `$price_kwh` in €/kWh (USR-03)
    - Kostenstelle: `$cost_center` (USR-04)
    - Speicherung: Dolibarr extrafields oder eigene Tabelle (hier vereinfacht gezeigt)
  </action>
  <acceptance_criteria>
    - wallboxbilling_setup.php enthält "GETPOST('rfid_hex" für RFID-Input (SEC-05)
    - wallboxbilling_setup.php verwendet "hash('sha256', $rfid_hex)" für Hashing (SEC-02)
    - wallboxbilling_setup.php hat Input-Felder für price_kwh und cost_center (USR-03, USR-04)
    - wallboxbilling_setup.php logged "substr($rfid_hash, 0, 16)" (SEC-01: nur Hash-Teil)
    - wallboxbilling_setup.php listet Benutzer mit "WHERE u.statut = 1" (USR-01: aktive Benutzer)
  </acceptance_criteria>
  <verify>grep -c "GETPOST('rfid_hex" Dolibarr/htdocs/custom/wallboxbilling/admin/wallboxbilling_setup.php && grep -c "hash('sha256'" Dolibarr/htdocs/custom/wallboxbilling/admin/wallboxbilling_setup.php && grep -c "price_kwh" Dolibarr/htdocs/custom/wallboxbilling/admin/wallboxbilling_setup.php</verify>
  <done>Admin-Interface erweitert für Benutzer-RFID-Verwaltung, Preise und Kostenstellen (USR-01 bis USR-05, SEC-05)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Dolibarr | RFID-Hex, Preise, Kostenstellen via Formular (GETPOST validiert) |
| RFID Storage → DB | Nur SHA-256 Hash in Datenbank (keine Klartext-Logs, SEC-01, SEC-02) |
| Admin UI → DB | SQL-Queries über $db->query() mit $db->escape() (SEC-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | wallboxbilling.class.php / wallboxbilling_setup.php | mitigate | RFID nur als SHA-256 Hash speichern und loggen (SEC-01, SEC-02). `dol_syslog()` immer mit substr($hash, 0, 16). Niemals RFID-Klartext in Logs oder DB. |
| T-02-07 | Tampering | RFID Input | mitigate | GETPOST('rfid_hex', 'alpha') für Input-Validierung. Hash-Berechnung server-side (SEC-05). |
| T-02-08 | SQL Injection | wallboxbilling.class.php | mitigate | $db->escape() für Strings, (int)/(float) Casts für Zahlen, Parameterized Queries so möglich (SEC-05). |
| T-02-09 | Spoofing | RFID Hash Verification | mitigate | `hash_equals()` für timing-safe Vergleich in verifyRfidHash(). |
| T-02-10 | Information Disclosure | User Extrafields | mitigate | RFID-Hash in extrafields/DB, keine Klartext-Speicherung. Zugriff nur für Admins (wallboxbilling.admin Recht, D-08). |
</threat_model>

<verification>
- [ ] `grep -c "CREATE TABLE llx_wallbox_sessions" Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.sql` >=1
- [ ] `grep -c "rfid_hash VARCHAR(64)" Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.sql` >=1
- [ ] `grep -c "CREATE INDEX" Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallbox_sessions.key.sql` >=3
- [ ] `grep -c "class WallboxBilling" Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php` >=1
- [ ] `grep -c "function createSession" Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php` >=1
- [ ] `grep -c "hash('sha256'" Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php` >=1
- [ ] `grep -c "GETPOST('rfid_hex" Dolibarr/htdocs/custom/wallboxbilling/admin/wallboxbilling_setup.php` >=1
- [ ] `grep -c "price_kwh" Dolibarr/htdocs/custom/wallboxbilling/admin/wallboxbilling_setup.php` >=1
- [ ] PHP-Syntax-Check: `php -l Dolibarr/htdocs/custom/wallboxbilling/class/wallboxbilling.class.php`
</verification>

<success_criteria>
- [ ] Datenbanktabelle llx_wallbox_sessions erstellt mit allen Pflichtfeldern (DB-01)
- [ ] Indizes auf rfid_hash, fk_user, start_time, status (DB-02)
- [ ] DAO-Klasse WallboxBilling mit createSession, endSession, fetch Methoden
- [ ] RFID-Hashing mit SHA-256 (hash('sha256')) implementiert (SEC-02, D-19)
- [ ] SQL-Injection Prävention mit $db->escape() und GETPOST() (SEC-05)
- [ ] Admin-Interface listet Benutzer mit RFID-Input, Preis und Kostenstelle (USR-01 bis USR-05)
- [ ] RFID wird NUR als Hash gespeichert (keine Klartext-Logs, SEC-01)
- [ ] Timing-safe Hash-Vergleich mit hash_equals() implementiert
- [ ] Dolibarr Module kann Benutzer für Wallbox aktivieren (USR-01)
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-tracking/02-02-SUMMARY.md`
</output>
