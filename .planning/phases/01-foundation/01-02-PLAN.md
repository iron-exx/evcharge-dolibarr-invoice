---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php
  - Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallboxbilling_sessions.sql
  - Dolibarr/htdocs/custom/wallboxbilling/admin.php
  - Dolibarr/htdocs/custom/wallboxbilling/index.php
  - Dolibarr/htdocs/custom/wallboxbilling/card.php
  - Dolibarr/htdocs/custom/wallboxbilling/bill.php
  - Dolibarr/htdocs/custom/wallboxbilling/langs/de_DE/wallboxbilling.lang
  - Dolibarr/htdocs/custom/wallboxbilling/img/wallbox.png
autonomous: true
requirements: [DB-03, SEC-04]
user_setup: []
must_haves:
  truths:
    - "Dolibarr Admin kann Wallbox-Modul aus der Modul-Liste aktivieren"
    - "Modul-Aktivierung erstellt llx_wallboxbilling_sessions Tabelle und richtet Berechtigungen ein"
    - "Drei Berechtigungen verfügbar: wallboxbilling.user, wallboxbilling.admin, wallboxbilling.billing"
    - "Die 4 Frontend-Seiten sind aufrufbar (admin, index, card, bill)"
  artifacts:
    - path: "Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php"
      provides: "Modul-Descriptor mit Rechten und Tabellen-Definition"
      contains: "function init()|function install()"
    - path: "Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallboxbilling_sessions.sql"
      provides: "SQL für llx_wallboxbilling_sessions Tabelle"
      contains: "CREATE TABLE|llx_wallboxbilling_sessions"
    - path: "Dolibarr/htdocs/custom/wallboxbilling/admin.php"
      provides: "Admin-Konfiguration mit Rechte-Verwaltung"
      contains: "wallboxbilling.admin|SETUP_ADMIN"
  key_links:
    - from: "modWallboxbilling.class.php"
      to: "llx_wallboxbilling_sessions.sql"
      via: "install() methode"
      pattern: "sql/llx_wallboxbilling_sessions.sql"
    - from: "admin.php"
      to: "Dolibarr Core"
      via: "SETUP_ADMIN, GETPOST()"
      pattern: "SETUP_ADMIN|GETPOST"
---

<objective>
Erstellt das Dolibarr Module Skeleton (wallboxbilling) mit Tabellen-Definition, Berechtigungen (SEC-04) und 4 Frontend-Seiten.

Purpose: Foundation für alle Dolibarr-Features (User Management, Billing, Invoicing). Ohne Modul-Skeleton können keine Datenbanktabellen oder PHP-Seiten für Wallbox-Abrechnung erstellt werden.

Output: Installierbares Dolibarr Modul mit Datenbank-Tabelle, Berechtigungen und Grundgerüst der Frontend-Seiten.
</objective>

<execution_context>
@/home/roto/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/roto/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/roto/.planning/PROJECT.md
@/home/roto/.planning/ROADMAP.md
@/home/roto/.planning/REQUIREMENTS.md
@/home/roto/.planning/phases/01-foundation/01-CONTEXT.md
@/home/roto/.planning/research/STACK.md
@/home/roto/AGENTS.md

# Dolibarr Reference Module
~/projects/Wallbox-Dolibarr/Dolibarr/noumainventoryapp/ — Vorlage für Modul-Struktur

# Dolibarr Modul-Verzeichnis (aus PROJECT.md)
Dolibarr/htdocs/custom/wallboxbilling/ — Zielverzeichnis für wallboxbilling Modul

# PHP Stack (aus AGENTS.md + STACK.md)
PHP 8.1+, Dolibarr 21.x-22.x, TCPDF für PDF
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dolibarr Modul-Descriptor und SQL-Tabelle (DB-03)</name>
  <files>Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php, Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallboxbilling_sessions.sql</files>
  <read_first>
    - ~/projects/Wallbox-Dolibarr/Dolibarr/noumainventoryapp/core/modules/modNoumaInventoryApp.class.php (Vorlage für Descriptor)
    - ~/projects/Wallbox-Dolibarr/Dolibarr/noumainventoryapp/sql/ (SQL-Dateien als Vorlage)
    - @/home/roto/.planning/phases/01-foundation/01-CONTEXT.md (D-05, D-06, D-07, DB-01 Vorbereitung)
  </read_first>
  <action>
    Erstelle das Dolibarr Modul-Grundgerüst (D-05, D-06, D-07, DB-03):

    1. **Verzeichnisstruktur erstellen**:
    ```bash
    mkdir -p Dolibarr/htdocs/custom/wallboxbilling/core/modules
    mkdir -p Dolibarr/htdocs/custom/wallboxbilling/sql
    mkdir -p Dolibarr/htdocs/custom/wallboxbilling/admin
    mkdir -p Dolibarr/htdocs/custom/wallboxbilling/langs/de_DE
    mkdir -p Dolibarr/htdocs/custom/wallboxbilling/img
    ```

    2. **modWallboxbilling.class.php** (Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php):
    ```php
    <?php
    /*/
    /*  modWallboxbilling.class.php - Wallbox Billing Modul Descriptor
    /*  
    /*  @author    Wallbox-Dolibarr Team
    /*  @version   1.0.0
    /*/

    require_once DOL_DOCUMENT_ROOT.'/core/modules/DolibarrModules.class.php';

    /**
     * Klasse für Wallbox Billing Modul
     */
    class modWallboxbilling extends DolibarrModules
    {
        /**
         * Konstruktor
         */
        public function __construct($db)
        {
            global $langs, $conf;

            $this->db = $db;
            $this->numero = 104000; // Modul-Nummer (frei wählbar, > 100000)
            $this->rights_class = 'wallboxbilling';
            $this->family = "financial"; // Familie: Finanzen
            $this->module_position = 80; // Position im Menü
            
            $this->name = array(
                'en_US' => 'Wallbox Billing',
                'de_DE' => 'Wallbox-Abrechnung'
            );
            
            $this->description = array(
                'en_US' => 'RFID-based billing for EV charging sessions',
                'de_DE' => 'RFID-basierte Abrechnung von Wallbox-Ladevorgängen'
            );
            
            $this->version = '1.0.0';
            $this->const_name = 'MAIN_MODULE_'.strtoupper($this->name['en_US']);
            $this->special = 0;
            $this->picto = 'wallbox@wallboxbilling'; // Icon aus img/ Verzeichnis
            
            // Abhängigkeiten
            $this->depends = array(); // Keine besonderen Abhängigkeiten
            $this->requiredby = array();
            $this->conflictwith = array();
            $this->langfiles = array("wallboxbilling.lang");
            
            // Modul-Initialisierung
            $this->init();
        }

        /**
         * Modul-Initialisierung (D-07, DB-03)
         */
        public function init()
        {
            // Tabellen erstellen
            $sql = array();
            $sql[] = file_get_contents(DOL_DOCUMENT_ROOT.'/custom/wallboxbilling/sql/llx_wallboxbilling_sessions.sql');
            
            return $this->__construct($this->db);
        }

        /**
         * Modul-Installation
         */
        public function install()
        {
            global $db, $conf;
            
            $error = 0;
            
            // SQL-Dateien ausführen
            $sql_file = DOL_DOCUMENT_ROOT.'/custom/wallboxbilling/sql/llx_wallboxbilling_sessions.sql';
            if (file_exists($sql_file)) {
                $result = $db->query(file_get_contents($sql_file));
                if (!$result) {
                    dol_syslog("WallboxBilling install error: ".$db->lasterror, LOG_ERR);
                    $error++;
                }
            }
            
            // Berechtigungen einrichten (D-08, SEC-04)
            $this->insert_permissions();
            
            return ($error == 0) ? 1 : 0;
        }

        /**
         * Modul-Deinstallation
         */
        public function uninstall()
        {
            global $db;
            
            $error = 0;
            
            // Berechtigungen entfernen
            $this->delete_permissions();
            
            return ($error == 0) ? 1 : 0;
        }
    }
    ?>

    **WICHTIG**: Die tatsächliche Tabellenerstellung erfolgt über die SQL-Datei (siehe unten). 
    Der Descriptor verweist darauf in `install()`.

    3. **llx_wallboxbilling_sessions.sql** (Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallboxbilling_sessions.sql):
    ```sql
    -- Wallbox Billing Sessions Tabelle (D-07, DB-03, DB-01 Vorbereitung)
    -- Erstellt die llx_wallboxbilling_sessions Tabelle für Ladevorgänge
    
    CREATE TABLE IF NOT EXISTS `llx_wallboxbilling_sessions` (
      `rowid` INTEGER PRIMARY KEY AUTOINCREMENT,
      `user_id` INTEGER NOT NULL DEFAULT 0,
      `rfid_hash` VARCHAR(128) NOT NULL DEFAULT '', -- SHA-256 Hash (64 Zeichen), Vorbereitung SEC-02
      `wallbox_id` VARCHAR(50) NOT NULL DEFAULT '', -- Vorbereitung EXT-01 (Multi-Wallbox)
      `start_time` DATETIME NULL DEFAULT NULL,
      `end_time` DATETIME NULL DEFAULT NULL,
      `kwh` DECIMAL(10,3) NOT NULL DEFAULT 0.000, -- Verbrauchte Energie in kWh
      `price_per_kwh` DECIMAL(10,4) NOT NULL DEFAULT 0.0000, -- Nutzerspezifischer Preis
      `total_cost` DECIMAL(10,2) NOT NULL DEFAULT 0.00, -- Gesamtkosten
      `status` VARCHAR(20) NOT NULL DEFAULT 'active', -- 'active', 'completed', 'cancelled'
      `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
      `updated_at` DATETIME NULL DEFAULT NULL
    );

    -- Indizes für performante Abfragen (DB-02 Vorbereitung)
    CREATE INDEX IF NOT EXISTS `idx_wallbox_sessions_rfid` ON `llx_wallboxbilling_sessions` (`rfid_hash`);
    CREATE INDEX IF NOT EXISTS `idx_wallbox_sessions_user` ON `llx_wallboxbilling_sessions` (`user_id`);
    CREATE INDEX IF NOT EXISTS `idx_wallbox_sessions_start` ON `llx_wallboxbilling_sessions` (`start_time`);
    CREATE INDEX IF NOT EXISTS `idx_wallbox_sessions_status` ON `llx_wallboxbilling_sessions` (`status`);

    -- Kommentare
    ALTER TABLE `llx_wallboxbilling_sessions` COMMENT 'Wallbox Ladevorgänge (Sessions) für Abrechnung';
    ```

    **HINWEIS**: In MariaDB/MySQL muss `AUTOINCREMENT` durch `AUTO_INCREMENT` ersetzt werden.
    Die Tabelle ist eine Vorbereitung für DB-01 (Phase 2) und enthält alle Pflichtfelder.
  </action>
  <acceptance_criteria>
    - modWallboxbilling.class.php enthält "class modWallboxbilling extends DolibarrModules"
    - modWallboxbilling.class.php enthält "function install()" mit SQL-Aufruf
    - llx_wallboxbilling_sessions.sql enthält "CREATE TABLE IF NOT EXISTS \`llx_wallboxbilling_sessions\`"
    - llx_wallboxbilling_sessions.sql enthält "rfid_hash VARCHAR(128)" (für SHA-256 Hash)
    - llx_wallboxbilling_sessions.sql enthält mindestens 3 CREATE INDEX Statements
    - Verzeichnisse core/modules/, sql/, admin/, langs/, img/ existieren
  </acceptance_criteria>
  <verify>test -f Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php && grep -c "class modWallboxbilling" Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php && grep -c "CREATE TABLE" Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallboxbilling_sessions.sql</verify>
  <done>Modul-Descriptor und SQL-Tabelle erstellt, Verzeichnisstruktur angelegt</done>
</task>

<task type="auto">
  <name>Task 2: Frontend-Seiten, Berechtigungen (SEC-04) und Deutsche Sprache</name>
  <files>Dolibarr/htdocs/custom/wallboxbilling/admin.php, Dolibarr/htdocs/custom/wallboxbilling/index.php, Dolibarr/htdocs/custom/wallboxbilling/card.php, Dolibarr/htdocs/custom/wallboxbilling/bill.php, Dolibarr/htdocs/custom/wallboxbilling/langs/de_DE/wallboxbilling.lang, Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php, Dolibarr/htdocs/custom/wallboxbilling/img/wallbox.png</files>
  <read_first>
    - ~/projects/Wallbox-Dolibarr/Dolibarr/noumainventoryapp/admin.php (Vorlage für Admin-Seite)
    - ~/projects/Wallbox-Dolibarr/Dolibarr/noumainventoryapp/index.php (Vorlage für Liste)
    - @/home/roto/.planning/phases/01-foundation/01-CONTEXT.md (D-08, D-21, D-22, D-23, D-24, SEC-04)
    - @/home/roto/AGENTS.md (Sprache: Deutsch, Stack: PHP 8.1+)
  </read_first>
  <action>
    Erstelle 4 Frontend-Seiten, Berechtigungen und deutsche Sprachdatei (D-08, D-21, D-22, D-23, D-24, SEC-04):

    1. **Berechtigungen in modWallboxbilling.class.php ergänzen** (D-08, SEC-04):
    
    In der `__construct()` Methode von modWallboxbilling.class.php, füge vor `$this->init()` hinzu:
    ```php
    // Berechtigungen definieren (D-08, SEC-04)
    $this->rights = array();
    
    $r = 0;
    
    // wallboxbilling.user - Normale Nutzer (können eigene Sessions sehen)
    $this->rights[$r][0] = 104001; // ID für Berechtigung
    $this->rights[$r][1] = 'View own charging sessions'; // Beschreibung (en)
    $this->rights[$r][2] = 'r'; // Leserecht
    $this->rights[$r][3] = 0; // Nicht aktiv standardmäßig
    $this->rights[$r][4] = 'wallboxbilling.user'; // Berechtigungs-Key
    $r++;
    
    // wallboxbilling.admin - Admins (können alle Sessions verwalten)
    $this->rights[$r][0] = 104002;
    $this->rights[$r][1] = 'Manage all charging sessions and users';
    $this->rights[$r][2] = 'w'; // Schreibrecht
    $this->rights[$r][3] = 0;
    $this->rights[$r][4] = 'wallboxbilling.admin';
    $r++;
    
    // wallboxbilling.billing - Billing (können Abrechnungen erstellen)
    $this->rights[$r][0] = 104003;
    $this->rights[$r][1] = 'Create monthly billing and invoices';
    $this->rights[$r][2] = 'w';
    $this->rights[$r][3] = 0;
    $this->rights[$r][4] = 'wallboxbilling.billing';
    $r++;
    ```

    2. **admin.php** (Dolibarr/htdocs/custom/wallboxbilling/admin.php):
    ```php
    <?php
    /*/
    /*  admin.php - Wallbox Billing Administration
    /*  
    /*  @author    Wallbox-Dolibarr Team
    /*/

    require_once '../../../main.inc.php';
    require_once '../core/modules/modWallboxbilling.class.php';

    // Berechtigungsprüfung (SEC-04, D-24)
    if (!$user->rights->wallboxbilling->admin) {
        accessforbidden();
    }

    $langs->load('wallboxbilling.lang');

    $page_name = 'WallboxBillingSetup';
    $page_title = $langs->trans('WallboxBillingSetup');

    llxHeader('', $page_title);

    $form = new Form($db);

    // Admin-Menü
    print load_fiche_titre($page_title, '', 'title_setup.png');

    // Konfiguration (Vorbereitung für Phase 4: Billing)
    print '<form method="POST" action="'.$_SERVER['PHP_SELF'].'">';
    print '<input type="hidden" name="token" value="'.newToken().'">';
    print '<input type="hidden" name="action" value="update">';

    print '<table class="noborder centpercent">';
    print '<tr class="liste_titre">';
    print '<td colspan="2">'.$langs->trans('WallboxConfiguration').'</td>';
    print '</tr>';

    // Standard kWh-Preis
    print '<tr><td>'.$langs->trans('DefaultPricePerKwh').'</td>';
    print '<td><input type="text" name="WALLBOXBILLING_DEFAULT_PRICE" value="'.getDolGlobalString('WALLBOXBILLING_DEFAULT_PRICE').'"></td></tr>';

    print '</table>';

    print '<div class="center"><input type="submit" class="button" value="'.$langs->trans('Save').'"></div>';
    print '</form>';

    // Rechte-Verwaltung anzeigen (SEC-04)
    print '<br>';
    print load_fiche_titre($langs->trans('Permissions'), '', 'title_setup.png');
    print '<table class="noborder centpercent">';
    print '<tr class="liste_titre"><td>'.$langs->trans('Permission').'</td><td>'.$langs->trans('Description').'</td></tr>';
    print '<tr><td>wallboxbilling.user</td><td>'.$langs->trans('ViewOwnSessions').'</td></tr>';
    print '<tr><td>wallboxbilling.admin</td><td>'.$langs->trans('ManageAllSessions').'</td></tr>';
    print '<tr><td>wallboxbilling.billing</td><td>'.$langs->trans('CreateBilling').'</td></tr>';
    print '</table>';

    llxFooter();
    ?>
    ```

    3. **index.php** (Dolibarr/htdocs/custom/wallboxbilling/index.php):
    ```php
    <?php
    /*/
    /*  index.php - Liste der Ladevorgänge (Skeleton)
    /*/

    require_once '../../../main.inc.php';
    $langs->load('wallboxbilling.lang');

    llxHeader('', $langs->trans('WallboxSessions'));

    print load_fiche_titre($langs->trans('WallboxSessions'), '', 'title_wallbox.png');

    // Vorbereitung für Phase 2: Sessions anzeigen
    print '<p>'.$langs->trans('SessionsWillBeDisplayedHere').'</p>';

    llxFooter();
    ?>
    ```

    4. **card.php** (Dolibarr/htdocs/custom/wallboxbilling/card.php):
    ```php
    <?php
    /*/
    /*  card.php - User-Link zu RFID (Skeleton, Vorbereitung USR-02)
    /*/

    require_once '../../../main.inc.php';
    $langs->load('wallboxbilling.lang');

    llxHeader('', $langs->trans('LinkUserToRFID'));

    print load_fiche_titre($langs->trans('LinkUserToRFID'), '', 'title_wallbox.png');

    // Vorbereitung für Phase 2: User mit RFID verknüpfen
    print '<p>'.$langs->trans('UserRFIDLinkWillBeHere').'</p>';

    llxFooter();
    ?>
    ```

    5. **bill.php** (Dolibarr/htdocs/custom/wallboxbilling/bill.php):
    ```php
    <?php
    /*/
    /*  bill.php - Abrechnung-Vorschau (Skeleton, Vorbereitung BIL-01)
    /*/

    require_once '../../../main.inc.php';
    $langs->load('wallboxbilling.lang');

    if (!$user->rights->wallboxbilling->billing) {
        accessforbidden();
    }

    llxHeader('', $langs->trans('MonthlyBilling'));

    print load_fiche_titre($langs->trans('MonthlyBilling'), '', 'title_wallbox.png');

    // Vorbereitung für Phase 4: Abrechnung
    print '<p>'.$langs->trans('BillingPreviewWillBeHere').'</p>';

    llxFooter();
    ?>
    ```

    6. **Deutsche Sprachdatei** (Dolibarr/htdocs/custom/wallboxbilling/langs/de_DE/wallboxbilling.lang) (D-23):
    ```gettext
    # Wallbox Billing Sprachdatei (Deutsch)
    # Generiert: 2026-05-04

    WallboxBillingSetup=Wallbox-Abrechnung Konfiguration
    WallboxConfiguration=Wallbox Konfiguration
    DefaultPricePerKwh=Standardpreis pro kWh (€)
    Permissions=Berechtigungen
    Permission=Berechtigung
    Description=Beschreibung
    WallboxSessions=Wallbox Ladevorgänge
    LinkUserToRFID=Benutzer mit RFID verknüpfen
    MonthlyBilling=Monatliche Abrechnung
    ViewOwnSessions=Eigene Ladevorgänge ansehen
    ManageAllSessions=Alle Ladevorgänge verwalten
    CreateBilling=Abrechnungen erstellen
    SessionsWillBeDisplayedHere=Hier werden die Ladevorgänge angezeigt.
    UserRFIDLinkWillBeHere=Hier können Benutzer mit RFID-Karten verknüpft werden.
    BillingPreviewWillBeHere=Hier wird die Abrechnungsvorschau angezeigt.
    Save=Speichern
    ```

    7. **Wallbox Icon** (Dolibarr/htdocs/custom/wallboxbilling/img/wallbox.png):
    - Erstelle eine leere Datei als Platzhalter (in Produktion durch echtes PNG ersetzen):
    ```bash
    touch Dolibarr/htdocs/custom/wallboxbilling/img/wallbox.png
    ```
  </action>
  <acceptance_criteria>
    - modWallboxbilling.class.php enthält "wallboxbilling.user", "wallboxbilling.admin", "wallboxbilling.billing" (SEC-04, D-08)
    - admin.php enthält "SETUP_ADMIN" oder Berechtigungsprüfung mit "$user->rights->wallboxbilling"
    - admin.php verwendet "GETPOST()" oder "$_SERVER['PHP_SELF']" (D-24)
    - index.php, card.php, bill.php existieren und enthalten "llxHeader"
    - wallboxbilling.lang enthält mindestens 10 deutsche Übersetzungen (D-23)
    - img/wallbox.png existiert
    - Deutsche Sprachdatei ist in langs/de_DE/ (nicht en_US/)
  </acceptance_criteria>
  <verify>grep -c "wallboxbilling.admin" Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php && grep -c "llxHeader" Dolibarr/htdocs/custom/wallboxbilling/index.php && test -f Dolibarr/htdocs/custom/wallboxbilling/langs/de_DE/wallboxbilling.lang && wc -l Dolibarr/htdocs/custom/wallboxbilling/langs/de_DE/wallboxbilling.lang | awk '{print $1}'</verify>
  <done>4 Frontend-Seiten, 3 Berechtigungen (SEC-04), deutsche Sprachdatei und Icon erstellt</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dolibarr Module → Dolibarr Core | Modul nutzt $db, GETPOST(), accessforbidden() |
| User Input → PHP Forms | GETPOST() Validierung (D-24, SEC-05 Vorbereitung) |
| RFID Hash → Datenbank | SHA-256 Hash in llx_wallboxbilling_sessions.rfid_hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | modWallboxbilling.class.php | mitigate | RFID nur als SHA-256 Hash speichern (rfid_hash VARCHAR(128), D-19). Keine Klartext-Logs mit dol_syslog() (D-18). |
| T-01-06 | Tampering | admin.php Forms | mitigate | GETPOST() Standard für Formular-Validierung nutzen (D-24, SEC-05 Vorbereitung). |
| T-01-07 | Elevation of Privilege | Permissions | mitigate | Drei Berechtigungen korrekt implementiert (wallboxbilling.admin für admin.php, wallboxbilling.billing für bill.php, SEC-04, D-08). |
| T-01-08 | SQL Injection | llx_wallboxbilling_sessions.sql | mitigate | In Phase 2: $db->escape() und Prepared Statements nutzen (SEC-05). In Phase 1 nur Tabelle erstellen. |
</threat_model>

<verification>
- [ ] `test -f Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php` → TRUE
- [ ] `grep -c "function install()" Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php` >= 1
- [ ] `grep -c "CREATE TABLE" Dolibarr/htdocs/custom/wallboxbilling/sql/llx_wallboxbilling_sessions.sql` >= 1
- [ ] `grep -c "wallboxbilling.billing" Dolibarr/htdocs/custom/wallboxbilling/core/modules/modWallboxbilling.class.php` >= 1
- [ ] `test -f Dolibarr/htdocs/custom/wallboxbilling/langs/de_DE/wallboxbilling.lang` → TRUE
- [ ] `php -l Dolibarr/htdocs/custom/wallboxbilling/admin.php` → kein Syntaxfehler
</verification>

<success_criteria>
- [ ] Dolibarr Modul wallboxbilling erstellt (modWallboxbilling.class.php)
- [ ] llx_wallboxbilling_sessions Tabelle mit allen Pflichtfeldern (DB-01 Vorbereitung, DB-03)
- [ ] Drei Berechtigungen definiert: wallboxbilling.user, .admin, .billing (SEC-04, D-08)
- [ ] 4 Frontend-Seiten: admin.php, index.php, card.php, bill.php (D-21)
- [ ] admin.php prüft wallboxbilling.admin Berechtigung
- [ ] bill.php prüft wallboxbilling.billing Berechtigung
- [ ] Deutsche Sprachdatei in langs/de_DE/ mit Wallbox-Begriffen (D-23)
- [ ] wallbox.png Icon in img/ Verzeichnis (D-22)
- [ ] SQL-Datei enthält Indizes auf rfid_hash, user_id, start_time (DB-02 Vorbereitung)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
